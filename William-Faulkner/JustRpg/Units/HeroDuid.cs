namespace JustRpg.Units
{
    using System;

    class HeroDuid : Hero
    {
        public HeroDuid(string name, int hitP, int attP, int defP, int speed) : base(name, hitP, attP, defP, speed)
        {
        }
    }
}
